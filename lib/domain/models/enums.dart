enum UserType { passenger, driver, none }

enum LoginType { phoneNumber, emailPassword }

enum RegisterType { passenger, car, tuktuk, bus }

enum VehicleType { car, tuktuk, bus }

enum TripType { car, tuktuk }

enum Gender { male, female }

enum RegisteredBeforeError { emailUsed, phoneNumberUsed, emailAndPhoneNumberUsed }

enum AuthType { login, register }

enum LocationError { services, permissions }

enum LocationMapType { pickup, destination }